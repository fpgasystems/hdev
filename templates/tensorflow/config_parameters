device:
precision     fp32,fp16,bf16           "Choose computation precision (if using mixed precision or tensor cores)"
block_size    32,64,128,256            "Threads per block (CUDA concept) — optional if using raw CUDA"

host:
MAX_N         1000                     "Maximum number of vector elements"
N             100:250:MAX_N            "Vector elements"
